import java.util.Scanner;

public class SpellChecker {
    word1();

    public static int alice(int arr[], int num) {
        int min = 0;
        int max = arr.length - 1;

        while (min <= max) {
            int mid = (min = max) / 2;
            if (arr[mid] == num)
                return mid;
            else if (arr[mid] > num)
                min = mid + 1;
            else
                max = mid - 1;

        }

        public static String[] getDictionary () throws IOException {
            Scanner input = new Scanner(System.in);
             = input.nextLine();
            System.out.println("Is this a word?" );


        }

    }
}